unknown = None  # неопределённое значение переменной

# Логические переменные Boolean Type
flag_bool = False

# Числа Numeric Type
x_int = 5
y_float = 0.5
z_complex = 9 + 7j

# Последовательности Sequence Type
A_list = [1.94, 2 + 3j, 4]
T_tuple = (7, 4, "smth")
R_range = range(9, 17, 2)

# Строки Text Sequence Type
string_str = "abc"

# Бинарные списки Binary Sequence Type
bit_bytes = bytes(998)
array_bit_bytearray = bytearray([7, 9, 90, 34, 65, 34, 35])
smth_memoryview = memoryview(bytearray('ABC', "utf-8"))

# Множества Set Types
elem_set = {8, 9, 3, 5}
objects_frozenset = frozenset([0, 9, 3, 6])

# Словари Mapping Types
names_dict = {0: "Vasya", 1: "Petya", 2: "Katya"}

3 << 4
